# Local frontend development can leave this unset; the client falls back to http://localhost:8000.
# Render injects this automatically from the chronos-api service host in render.yaml.
#
# Use a host only, without https://, when setting this manually:
# VITE_API_URL=chronos-api.onrender.com
VITE_API_URL=
